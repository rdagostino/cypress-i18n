// Enabled features & configs for US Region.
import { ConfigAndFeaturesType } from '../featureTypes';

export const configAndFeatures: ConfigAndFeaturesType = {
  features: {
    enableLoginBanner: true,
  },
};
